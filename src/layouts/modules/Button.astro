---
interface Props {
  size?: "sm" | "md" | "lg";
  block?: boolean;
  style?: "outline" | "primary" | "link";
  classname?: string;
  [rest: string]: any;
}

const {
  size = "md",
  block = false,
  style = "primary",
  classname,
  ...rest
} = Astro.props;

const sizes = {
  sm: "px-3 py-1.5",
  md: "px-3.5 py-2.5",
  lg: "px-6 py-3",
};

const styles = {
  outline: "border-2 border-black hover:bg-black text-black hover:text-white",
  primary: "bg-black text-white dark:bg-white dark:text-black dark:hover:bg-slate-100 hover:bg-slate-900 border-2 border-transparent",
  link: "text-amber-900 font-semibold text-sm text-center transition hover:text-amber-600 focus:text-amber-600 focus:bg-neutral-200 focus:outline-none focus:shadow-outline hover:bg-neutral-200 dark:hover:bg-neutral-700 dark:focus:bg-neutral-700 dark:focus:text-amber-300 dark:hover:text-amber-300 dark:text-amber-600",
};
---

<button
  class:list={[
    "rounded-md text-center transition focus-visible:ring-2 ring-offset-2 ring-neutral-200",
    sizes[size],
    block && "w-full",
    styles[style],
    classname,
  ]}
  {...rest}
>
  <slot />
</button>